import styled from '@emotion/styled';
import tw from "tailwind.macro";
const Label = styled.label`${tw`text-lg`}`
 const ProductSortMainDiv= styled.select`${tw` h-10 ml-2 rounded-md bg-white border border-gray-400 hover:border-gray-700 focus:outline-none`}`

 export{ProductSortMainDiv,Label}